environment:
  matrix:
    - nodejs_version: "8"
    - nodejs_version: "10"
branches:
  only:
    - master
    - stable
install:
  - "yarn global add nyc eslint"
  - "yarn"
build: off
test_script:
  - "yarn test-ci"
version: "{build}"